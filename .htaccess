
# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>

# END WordPress

php_value memory_limit 128M
php_value upload_max_filesize 50M
php_value post_max_size 50M

# uncomment the following block when you are doing an upgrade and need a maintenance page
# the ip address is to allow the admin to get through the blockade. You will change 
# if the network you are on changes. This also lets all graphic files through
# so our themes will still load images for admins in maintenance mode
# RewriteCond %{REQUEST_URI} !/maintenance.html$
# RewriteCond %{REQUEST_FILENAME} !.*jpg$|.*gif$|.*png$ [NC]
# RewriteCond %{REMOTE_HOST} !^66\.60\.166\.155
# RewriteRule $ maintenance.html [R=302,L]